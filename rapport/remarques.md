# Remarques du dossier d'analyse

- page 1 : un utilisateur est d'office client et n'est pas admin ou collaborateur dès le départ
- possibilité de faire une ligne dans F2 pultot que F3
- page 4 : utilisateur de l'application : compte utilisateur plutot que applicatif
- page 6: diag cas utili : utilisateur va sur une appli pour utiliser une fonctinnalité, pas pour se connecter donc pour chaque fonctionnalité, mettre un include vers se connecter
- Gérer un trajet plutot que une bulle avec tout et après 3 extend vers creer, modifier et supprimer
- Oubli de rajouter le système de réduction dans FO1
- Pour voir le prix, soit il faut que le prix apparaisse tout de suite (donc changer en include) ou il faut demander une réservation pour le voir et donc il faut faire uen bulle intermédiaire "reserver"
- diag de classe : avoir des paramètres dans les méthodes avec l'API SNCF
- moins regrouper les fleches qui descendent vers les couches du bas
- endpoints de l'API : la priorité est de finaliser le listing des endpoints pour orienter notre projet
- mettre dans le rapport final un diagramme de la base de donnée